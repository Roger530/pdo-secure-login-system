# دليل النشر الدائم لموقع ويب (PHP & MySQL)

لتحويل مشروعك من بيئة تجريبية إلى **موقع ويب دائم**، تحتاج إلى اتباع خطوات محددة لنقله إلى "استضافة ويب" (Web Hosting). إليك الدليل الشامل للقيام بذلك.

---

## 1. اختيار الاستضافة المناسبة

بما أن مشروعك يعتمد على **PHP** و **MySQL**، فلديك عدة خيارات (مجانية ومدفوعة):

### خيارات مجانية (مثالية للمشاريع الطلابية):
| المزود | الميزات | الرابط |
|--------|---------|--------|
| **InfinityFree** | مساحة غير محدودة، دعم PHP/MySQL كامل، بدون إعلانات. | [infinityfree.com](https://www.infinityfree.com) |
| **000webhost** | مدعوم من Hostinger، سهل الاستخدام، يوفر لوحة تحكم cPanel. | [000webhost.com](https://www.000webhost.com) |
| **AeonFree** | استضافة سريعة للطلاب مع عرض نطاق ترددي غير محدود. | [aeonfree.com](https://aeonfree.com) |

### خيارات مدفوعة (للمشاريع الاحترافية والدائمة جداً):
- **Hostinger:** تبدأ الأسعار من حوالي 2$ شهرياً، توفر أداءً عالياً ونطاقاً (Domain) مجانياً.
- **Namecheap:** توفر استضافة مشتركة رخيصة وموثوقة جداً.

---

## 2. خطوات النشر (العملية)

بعد اختيار الاستضافة وإنشاء حساب، اتبع هذه الخطوات:

### الخطوة الأولى: إعداد قاعدة البيانات
1. ادخل إلى لوحة تحكم الاستضافة (cPanel أو ما يشابهها).
2. ابحث عن **MySQL Databases** وأنشئ قاعدة بيانات جديدة.
3. أنشئ مستخدم جديد لقاعدة البيانات وامنحه كافة الصلاحيات.
4. ابحث عن **phpMyAdmin**، اختر قاعدة البيانات التي أنشأتها، ثم اضغط على **Import**.
5. ارفع ملف `setup_database.sql` المرفق مع المشروع لتوليد الجداول.

### الخطوة الثانية: تعديل ملف التكوين (`config.php`)
افتح ملف `config.php` وقم بتحديث بيانات الاتصال لتطابق بيانات الاستضافة الجديدة:
```php
define('ENVIRONMENT', 'production'); // تغيير إلى production لإخفاء الأخطاء

define('DB_HOST', 'اسم_السيرفر_غالبا_localhost');
define('DB_NAME', 'اسم_قاعدة_البيانات_الجديدة');
define('DB_USER', 'اسم_المستخدم_الجديد');
define('DB_PASS', 'كلمة_المرور_الجديدة');
```

### الخطوة الثالثة: رفع الملفات
1. ابحث عن **File Manager** في لوحة التحكم، أو استخدم برنامج **FTP** مثل (FileZilla).
2. ادخل إلى المجلد الرئيسي (غالباً يكون `public_html` أو `htdocs`).
3. ارفع جميع ملفات المشروع الموجودة داخل مجلد `pdo_login_system`.

---

## 3. ميزات الأمان التي أضفتها لك للنشر الدائم

لقد قمت بتحديث الكود الخاص بك ليكون جاهزاً للعمل في بيئة حقيقية:
- **حماية CSRF:** أضفت "توكن أمان" لنماذج تسجيل الدخول والتسجيل لمنع الهجمات الخارجية.
- **أمان الجلسات:** تم ضبط الجلسات (Sessions) لتكون أكثر أماناً (HttpOnly & Secure).
- **إخفاء الأخطاء:** عند تغيير البيئة إلى `production` في ملف `config.php` لن تظهر أي أخطاء برمجية للمستخدمين (لحماية هيكل الكود).
- **ملف .htaccess:** أضفت ملفاً يمنع الوصول المباشر لملفات قاعدة البيانات ويحمي المجلدات.

---

## 4. الحصول على نطاق (Domain)

إذا كنت تريد رابطاً مثل `www.yourname.com`:
- **مجاني:** يمكنك استخدام النطاقات الفرعية التي توفرها الاستضافات المجانية (مثل `yoursite.infinityfreeapp.com`).
- **مدفوع:** يمكنك شراء نطاق `.com` أو `.net` من مواقع مثل **Namecheap** أو **GoDaddy** وربطه باستضافتك.

---

## 5. نصائح إضافية
- **SSL (HTTPS):** تأكد من تفعيل شهادة الأمان SSL (القفل الأخضر) في استضافتك؛ معظم الاستضافات المجانية والمدفوعة توفرها مجاناً عبر (Let's Encrypt).
- **النسخ الاحتياطي:** قم دائماً بأخذ نسخة احتياطية من قاعدة البيانات والملفات بشكل دوري.

**مبروك! مشروعك الآن جاهز ليكون موقعاً عالمياً دائماً.**
